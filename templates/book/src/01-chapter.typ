#import "setup.typ": *

#show: chapter.with("First chapter")

== A section

A chapter starts on a fresh page and opens with an outline of its own sections,
so a later chapter cites an earlier one rather than repeating it.

== Another section

An equation is numbered where something references it, as @euler is,
$ e^(ii pi) + 1 = 0. $ <euler>
